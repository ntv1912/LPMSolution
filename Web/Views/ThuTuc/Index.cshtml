@model IEnumerable<Infrastructure.Models.ThuTuc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaThuTuc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenThuTuc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MoTa)
            </th>
            <
            <th>
                @Html.DisplayNameFor(model => model.LinhVuc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MaThuTuc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TenThuTuc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MoTa)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LinhVuc.Id)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
